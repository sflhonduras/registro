// Se actualiza a mano cada vez que se hace un despliegue importante — junto con una
// entrada nueva en CHANGELOG.md (en la raíz del proyecto) y una etiqueta de Git
// (ej. "git tag v1.0.0") en ese mismo commit, para poder encontrarlo fácil en el futuro.
export const VERSION = 'v1.0.0';
export const FECHA_VERSION = '9 de agosto de 2026';
